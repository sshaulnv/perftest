name: Build and Test perftest on SLES 15

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: sles-15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo zypper update -y
          sudo zypper install -y \
            gcc \
            gcc-c++ \
            make \
            autoconf \
            automake \
            libtool \
            pkg-config \
            rdma-core-devel \
            libibverbs-devel \
            librdmacm-devel \
            libibumad-devel \
            pciutils-devel

      - name: Run autogen.sh
        run: ./autogen.sh

      - name: Configure the build
        run: ./configure

      - name: Build perftest
        run: make
    
      - name: Install perftest
        run: sudo make install 